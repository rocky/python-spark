from setuptools import setup
setup(
       description        = "gdb location example",
       install_requires   = ['spark_parser'],
       name               = 'gdbloc',
       version            = '1.0'
)
