from setuptools import setup
setup(
       description        = "gdb location example",
       install_requires   = ['spark_parser >= 1.7.0'],
       name               = 'gdbloc',
       version            = '1.0'
)
